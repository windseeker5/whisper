# Core dependencies (required) - NO WHISPER, NO TORCH
pyaudio>=0.2.11
sounddevice>=0.4.6
soundfile>=0.12.1
pydub>=0.25.1
pynput>=1.7.6
pyperclip>=1.8.2
numpy>=1.24.0
audioop-lts>=0.2.1
psutil>=5.9.0

# TUI dependencies (required for --tui mode)
textual>=0.50.0
rich>=13.7.0

# Vosk backend - Lightweight, fast (RECOMMENDED for 4GB RAM)
vosk>=0.3.45

# Audio processing (lightweight)
scipy>=1.10.0
